name: Temporal audience history and incremental equivalence
on: [push, pull_request]
permissions:
  contents: read
concurrency:
  group: temporal-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      - run: |
          python -m pip install -r requirements.lock
          python -m pip install --no-deps --no-build-isolation -e .
      - run: python scripts/verify.py
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: temporal-proof-${{ matrix.os }}
          path: |
            docs/evidence/verification.json
            docs/evidence/tests.xml
            docs/evidence/report.md
            docs/evidence/temporal-proof.json
            docs/evidence/timeline.csv
